/// CORE / CONSTANTS
/// Hard-coded text lives here, not scattered across widgets. Makes wording
/// changes (and future translation) easy.
class AppStrings {
  AppStrings._();

  static const String appName = 'Social Connect';
  static const String feedTitle = 'Feed';
  static const String loadError = 'Failed to load posts';
}
